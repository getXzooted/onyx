# /opt/onyx/config/assets.yml
assets:
  sentinel_dns:
    source: "https://raw.githubusercontent.com/stevenblack/hosts/master/hosts"
    path: "/etc/unbound/unbound.conf.d/onyx-sentinel.conf"
    type: "unbound_filter"
    action: "systemctl restart unbound"

  geoblock_high_risk:
    source: "https://raw.githubusercontent.com/herrbischoff/country-ip-blocks/master/ipv4/cn.zone"
    path: "/etc/onyx/firewall/geo_block.list"
    type: "ip_list"
    action: "/usr/local/bin/onyx network repair"

  yq_processor:
    source: "https://github.com/mikefarah/yq/releases/latest/download/yq_linux_arm"
    path: "/usr/local/bin/yq"
    type: "binary"
